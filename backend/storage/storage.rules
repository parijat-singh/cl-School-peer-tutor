rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // School logos — only school admins of that school can write; all school members can read
    match /schools/{domain}/{fileName} {
      allow read:  if request.auth != null && request.auth.token.schoolDomain == domain;
      allow write: if request.auth != null
                   && request.auth.token.role == 'schooladmin'
                   && request.auth.token.schoolDomain == domain
                   && request.resource.size < 500 * 1024  // 500KB max
                   && request.resource.contentType.matches('image/.*');
    }

    // Default deny
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
