version: "3.9"

# ─────────────────────────────────────────────────────────────────
# PeerTutor — Docker Compose
# Services: frontend (React), backend (Firebase emulators), nginx
# ─────────────────────────────────────────────────────────────────

services:
  # ── React Frontend (Vite dev server) ──────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    container_name: peertutor-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_FIREBASE_API_KEY=${FIREBASE_API_KEY}
      - VITE_FIREBASE_AUTH_DOMAIN=${FIREBASE_AUTH_DOMAIN}
      - VITE_FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID}
      - VITE_FIREBASE_STORAGE_BUCKET=${FIREBASE_STORAGE_BUCKET}
      - VITE_FIREBASE_MESSAGING_SENDER_ID=${FIREBASE_MESSAGING_SENDER_ID}
      - VITE_FIREBASE_APP_ID=${FIREBASE_APP_ID}
      - VITE_USE_EMULATORS=true
      - VITE_EMULATOR_HOST=localhost
    depends_on:
      - firebase-emulators
    networks:
      - peertutor-net
    restart: unless-stopped

  # ── Firebase Emulators (Auth, Firestore, Functions, Storage) ──
  firebase-emulators:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: peertutor-firebase
    ports:
      - "4000:4000"   # Emulator UI
      - "5001:5001"   # Functions
      - "8080:8080"   # Firestore
      - "9099:9099"   # Auth
      - "9199:9199"   # Storage
    volumes:
      - ./backend:/app
      - /app/functions/node_modules
      - firebase-data:/app/.firebase-data
    environment:
      # ── Emulator wiring (required) ──
      - FIRESTORE_EMULATOR_HOST=0.0.0.0:8080
      - FIREBASE_AUTH_EMULATOR_HOST=0.0.0.0:9099
      - FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID}
      # ── All secrets come from .env (NEVER hardcode here) ──
      - SENDGRID_API_KEY=${SENDGRID_API_KEY}
      - SENDGRID_FROM_EMAIL=${SENDGRID_FROM_EMAIL}
      - SENDGRID_FROM_NAME=${SENDGRID_FROM_NAME}
      - SENDGRID_TEMPLATE_BOOKING_CONFIRMATION=${SENDGRID_TEMPLATE_BOOKING_CONFIRMATION}
      - SENDGRID_TEMPLATE_CANCELLATION=${SENDGRID_TEMPLATE_CANCELLATION}
      - SENDGRID_TEMPLATE_REMINDER=${SENDGRID_TEMPLATE_REMINDER}
      - SENDGRID_TEMPLATE_RATING_PROMPT=${SENDGRID_TEMPLATE_RATING_PROMPT}
      - SENDGRID_TEMPLATE_WELCOME=${SENDGRID_TEMPLATE_WELCOME}
      - GOOGLE_CALENDAR_CLIENT_EMAIL=${GOOGLE_CALENDAR_CLIENT_EMAIL}
      - GOOGLE_CALENDAR_PRIVATE_KEY=${GOOGLE_CALENDAR_PRIVATE_KEY}
      - GOOGLE_CALENDAR_ID=${GOOGLE_CALENDAR_ID}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - SUPER_ADMIN_EMAIL=${SUPER_ADMIN_EMAIL}
    networks:
      - peertutor-net
    restart: unless-stopped

  # ── Nginx Reverse Proxy ─────────────────────────────────────────
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: peertutor-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - frontend
      - firebase-emulators
    networks:
      - peertutor-net
    restart: unless-stopped

networks:
  peertutor-net:
    driver: bridge

volumes:
  firebase-data:
    driver: local
